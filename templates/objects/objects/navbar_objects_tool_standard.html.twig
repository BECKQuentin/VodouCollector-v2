{% if is_granted('ROLE_ADMIN') %}
    <div class="position-absolute top-0 end-0 d-flex sort_item_toolbar px-2">
        <div>
            Ajouter
            <label for="media_form_name" class="d-flex">
                <span class="btn btn-vodou my-2"><i class="fa-solid fa-plus"></i></span>
                    {{ form_row(form.image) }}
            </label>
        </div>
    </div>
    <ul id="objects_media_input_file_name">
    </ul>
{% endif %}
